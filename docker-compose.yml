version: '3.9'

services:
  frontend:
    build: .
    container_name: frontend_app
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - "5173:5173"
    stdin_open: true
    tty: true
